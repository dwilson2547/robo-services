# TODO

Backlog imported from the retired todo store, 2026-07-06.

## High

- [ ] **Integrate WiFiManager into ESP32 datalogger** — Replace hardcoded WiFi credentials with WiFiManager captive portal. Add custom parameters for receiver IP and port so the device is fully self-configuring without re-flashing. Save custom params to Preferences flash alongside WiFi credentials.
- [ ] **Add reset button to ESP32 datalogger** — Wire GPIO 0 (built-in BOOT button on most DevKits) to wipe WiFiManager credentials on 3-second hold at boot. Calls wm.resetSettings() and relaunches the config portal. No extra hardware needed on most boards.
- [ ] **Replace JSON with MsgPack in ESP32 datalogger** — Swap ArduinoJson serializeJson for serializeMsgPack to reduce packet size ~50-60%. Field name strings disappear, floats become native binary. Update TCP receiver and Flink pipeline to deserialize MsgPack. Important before CAN bus raises message volume.
- [ ] **Replace UDP transport with MQTT in ESP32 datalogger** — Swap UDP publishPacket for MQTT publish using PubSubClient or AsyncMQTT. Deploy Mosquitto broker to robo-services Helm chart as the ingestion point. Mosquitto forwards to Iggy replacing the current UDP receiver. Use QoS 0 for low-overhead fire-and-forget (matches current UDP behavior) with option to bump to QoS 1 for reliability over internet. Fixes NAT/firewall issues when publishing from phone hotspot at track. Do after MsgPack (#50) since both touch the publish path.

## Medium

- [ ] **Try Adafruit GPS-RTK-SMA on the datalogger** — Swap in the ZED-F9P based Adafruit GPS-RTK-SMA breakout as the GPS source. Evaluate accuracy improvement and assess fit for the personal high-end logger build. Compare with current GPS at same location.
- [ ] **Add lap marker button to ESP32 datalogger** — Add a button (dedicated GPIO) that publishes a special message_type: lap_marker to the telemetry feed. User presses at start/finish line. Flink pipeline listens for marker messages to delimit lap windows for timing and analytics. This is the core of the open MyChron alternative.
- [ ] **Select GPS/IMU combo for field datalogger units** — Evaluate a mid-tier GPS + 6DOF IMU (with gyroscope) combo suitable for units sent to other users. Gyro needed for IMU feed filtering/fusion. Budget-conscious — not RTK. Should support same firmware as personal unit. Consider u-blox M8 series + ICM-42688-P or similar.

## Low

- [ ] **Research Ducati CAN bus connector and PIDs for 2013 Monster 696** — Identify the diagnostic connector type and pinout for the 2013 Monster 696 (Magneti Marelli ECU). Find community-documented CAN PIDs for lean angle, TC events, RPM, speed, gear position. Source or fabricate adapter cable to interface with ESP32/TJA1051 hardware. 2004 S4R uses pre-CAN serial protocol - out of scope.
- [ ] **Research KTM CAN bus connector and PIDs** — Identify diagnostic connector type and CAN PIDs for relevant KTM models (friend in Seattle). Determine which model years are supported. Same ESP32/TJA1051 hardware, different cable and DBC file.
- [ ] **Add SD card write buffer to ESP32 datalogger for WiFi dropout resilience** — Add SPI SD card module to buffer telemetry messages locally when WiFi is unavailable. Replay buffered messages to receiver when connection restores. Important for track use where hotspot coverage may be intermittent.

## Unprioritized (imported from workman drafts, 2026-07-06)

- [ ] **kreceiver mode suffix parsing and heartbeat integration** — Update kreceiver (src/can_pub_sub_probe/iggy_backend.py or equivalent) to strip -r/-t suffix from device_id before Iggy publish, stamp mode on message envelope headers, and call POST /api/devices/heartbeat debounced ~60s per device. Hardware (ESP32) must be publishing mode suffix before this is testable.
- [ ] **Flink jobs pipeline enablement check** — Update speed and lap Flink jobs to query GET /api/devices/{id}/mode/{mode}/pipelines at startup or per-checkpoint. Cache result with TTL. Skip device-message processing when pipeline not enabled for that device+mode combination.
- [ ] **CAN bus serial dump test sketch** — Quick standalone sketch to validate TJA1051 wiring and ESP32-S3 TWAI controller before any other work. No WiFi, no MessagePack, no publishing — just listen on the CAN bus and print raw frames to Serial. Wire ESP32-S3 TX/RX to TJA1051 TXD/RXD (3.3V-tolerant), TJA1051 CANH/CANL to OBD-II port pin 6/14 (HS-CAN). Use ESP32 TWAI driver (arduino-esp32 CAN library or direct IDF twai_ calls). Connect to vehicle ignition-on, verify known GM GlobalA packets appear: 0x0C9 (RPM/throttle/brake), 0x3E9 (vehicle speed), 0x4C1 (temps), 0x135 (gear). Print raw hex to Serial at 115200 baud. Exit criteria: live changing CAN frame data visible from vehicle on Serial monitor.
- [ ] **u-blox M9N GPS integration on ESP32-S3** — Replace GTU7 GPS module with u-blox M9N in the device sketch. M9N outputs NMEA by default — continue using TinyGPSPlus for parsing. Update UART pin assignments for ESP32-S3 Sense Edition (pin layout differs from original ESP32 dev board used with GTU7). Verify fix acquisition, location, altitude, speed, heading, HDOP, and satellite count fields populated correctly. Publish GPS frames to gps topic over UDP using same JSON schema as existing sketch. Baud rate: M9N defaults to 38400 (vs GTU7 9600) — update accordingly.
- [ ] **BNO085 IMU integration on ESP32-S3** — Replace MPU6050 with BNO085 IMU (Hillcrest Labs, also known as BNO08X family). Use Adafruit BNO08X Arduino library. BNO085 communicates via I2C (same bus as before) or SPI — use I2C for simplicity. Key upgrade over MPU6050: BNO085 has onboard sensor fusion (rotation vector / quaternion output) in addition to raw accel/gyro. Include rotation vector (quaternion), linear acceleration, and raw gyro in IMU frame. Update IMU JSON schema to include quaternion fields. Publish IMU frames to imu topic over UDP. Verify output stability and update rate on ESP32-S3 I2C pins.
- [ ] **Combined device sketch — GPS, IMU, and CAN publishing to topics** — Integrate all three validated sensor subsystems (M9N GPS, BNO085 IMU, TJA1051 CAN) into a single production-ready sketch for the ESP32-S3 Sense Edition. Follows existing sketch architecture: non-blocking loop, hardware serial for GPS, I2C for IMU, TWAI for CAN, WiFi UDP for publishing. Three publish topics: gps (1 Hz with fix, 5 Hz no-fix), imu (configurable rate, ~10-50 Hz), can (event-driven on frame receipt, filter to telemetry-relevant IDs: 0x0C9, 0x3E9, 0x4C1, 0x135, 0x348, 0x34A). CAN frames published as JSON: {id, dlc, data_hex, data_bytes[], timestamp}. Device ID derived from ESP32 MAC. LED status indicators for WiFi and GPS fix (existing heartbeat pattern). This is the baseline device firmware before MessagePack, MQTT, race mode buffering, and user config are added (those live in EPIC-011 tasks TASK-036/037).
